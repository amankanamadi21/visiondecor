# VisionDecor backend — Batch 1 (backend foundation) dependencies.
# AI-stage dependencies (ultralytics, torch, clip, etc.) are added when Batch 2 is built.

# --- Web framework (D012) ---
Flask==3.0.3
Flask-Cors==4.0.1

# --- Database (D013) ---
SQLAlchemy==2.0.35
psycopg2-binary==2.9.9
alembic==1.13.3
pgvector==0.3.4

# --- Auth (D014) ---
PyJWT==2.9.0
argon2-cffi==23.1.0

# --- Validation / config ---
pydantic==2.9.2
python-dotenv==1.0.1
python-magic==0.4.27

# --- Images ---
Pillow==10.4.0

# --- Testing ---
pytest==8.3.3
pytest-cov==5.0.0

# --- Dev ---
faker==29.0.0

# --- Batch 2: RAG embeddings (D006a) — CPU-only inference, no training ---
sentence-transformers==3.1.1

# --- Batch 3: generative visualization (D003/D023) ---
google-genai==2.22.0

# --- Batch 4: style recognition (D005) — CPU-only zero-shot, no training ---
open_clip_torch==3.3.0
scikit-learn==1.9.0

# --- Batch 5: Cloudflare Workers AI provider (D003) ---
requests==2.34.2

# --- Batch 6: real furniture detection + architectural segmentation
# (FR-2, Report Issue R-07) — CPU-only pretrained models, no fine-tuning ---
ultralytics==8.4.143
transformers==4.57.6
opencv-python-headless==5.0.0.93

# --- Batch 6: detection mAP evaluation only (evaluation/run_detection_study.py) ---
pycocotools==2.0.11
